# Harbor Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build Harbor, a $0 client-side HAR network-capture analyzer with a deterministic audit engine, an interactive waterfall, before/after diff, and a grounded BYO-key AI copilot, all in the browser with nothing uploaded.

**Architecture:** A single parse of the HAR feeds pure, unit-tested analysis modules in `lib/har/` (waterfall, weight, third-party, privacy, security, caching, protocol, audit, diff). A thin React UI renders the result; a Web Worker runs parse+analyze off the main thread with a synchronous fallback; the AI copilot grounds strictly on the deterministic `Analysis`.

**Tech Stack:** Next.js 16 (static export), React 19, Tailwind v4, TypeScript, Vitest. No backend.

**Spec:** `docs/superpowers/specs/2026-06-15-harbor-design.md`

**Phasing:** Phase 1 below is the clean one-day MVP target, written in full TDD detail. Phases 2-6 are execute-ready task outlines (concrete files, signatures, and key assertions); expand each into full bite-sized steps when that phase begins.

---

## File Structure

```
harbor/
  package.json, tsconfig.json, next.config.ts, postcss.config.mjs
  app/
    layout.tsx, globals.css, page.tsx          # single-capture dashboard
    compare/page.tsx                           # Phase 5 diff view
  components/
    Waterfall.tsx, WeightBreakdown.tsx, ThirdParties.tsx,
    Findings.tsx, ScoreHeader.tsx, Recommendations.tsx,
    RequestPanel.tsx, Copilot.tsx
  lib/
    har/
      types.ts        # normalized model
      parse.ts        # parseHar + helpers
      waterfall.ts    # timeline geometry
      weight.ts       # bytes by type/domain
      thirdparty.ts   # third-party grouping/classification
      privacy.ts      # secrets/PII/trackers (+cookie-sync/fp in P4)
      security.ts     # P4: header/cookie/mixed-content audit
      caching.ts      # P4: cache audit + repeat-view
      protocol.ts     # P4: http version/redirects/reuse
      audit.ts        # P2: recommendations + score + budgets
      diff.ts         # P5: compare two analyses
      analyze.ts      # orchestrator
      summary.ts      # toMarkdown report
      sample.ts       # baked sample HARs
      runParse.ts     # worker runner + sync fallback
      worker.ts       # off-main-thread parse+analyze
    ai/
      copilot.ts      # grounded BYO-key streaming copilot
  tests/
    *.test.ts         # one per pure module
```

---

# Phase 1: MVP core (one-day target)

Deliverable: drop a `.har` → parsed in a worker → dashboard with summary,
interactive-less waterfall, weight, third parties, privacy findings, grounded
copilot, and a Markdown download. Tests + static build green.

## Task 1: Scaffold the project

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`, `vitest.config.ts`, `app/layout.tsx`, `app/globals.css`, `app/page.tsx`

- [ ] **Step 1: Create `package.json`**

```json
{
  "name": "harbor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^16.2.9",
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^2"
  }
}
```

- [ ] **Step 2: Create `next.config.ts`**

```ts
import type { NextConfig } from "next";
const nextConfig: NextConfig = { output: "export" };
export default nextConfig;
```

- [ ] **Step 3: Create `postcss.config.mjs`**

```js
const config = { plugins: { "@tailwindcss/postcss": {} } };
export default config;
```

- [ ] **Step 4: Create `tsconfig.json`** (Next will normalize on first build)

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "allowJs": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

- [ ] **Step 5: Create `vitest.config.ts`**

```ts
import { defineConfig } from "vitest/config";
import { fileURLToPath } from "node:url";
export default defineConfig({
  resolve: { alias: { "@": fileURLToPath(new URL("./", import.meta.url)) } },
  test: { environment: "node" },
});
```

- [ ] **Step 6: Create `app/globals.css`**

```css
@import "tailwindcss";
body { background: #09090b; color: #e4e4e7; }
```

- [ ] **Step 7: Create `app/layout.tsx`**

```tsx
import type { Metadata } from "next";
import "./globals.css";
export const metadata: Metadata = {
  title: "Harbor — drop a .har, see what's slow",
  description: "Analyze a browser network capture: waterfall, weight, third parties, privacy leaks, and a scored audit. 100% in your browser. Nothing uploaded.",
};
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (<html lang="en"><body>{children}</body></html>);
}
```

- [ ] **Step 8: Create a placeholder `app/page.tsx`**

```tsx
export default function Home() {
  return <main className="p-6">Harbor</main>;
}
```

- [ ] **Step 9: Install and verify**

Run: `npm install && npm run build`
Expected: build succeeds, static export emitted to `out/`.

- [ ] **Step 10: Commit**

```bash
git init && git add -A
git commit -m "Scaffold Harbor: Next.js static export + Tailwind v4 + Vitest"
```
(Ensure `git config user.name`=krunal85, `user.email`=kp587372@gmail.com. Add a Next-style `.gitignore` excluding node_modules/.next/out/*.tsbuildinfo/.vercel before committing. No AI attribution.)

## Task 2: Core types

**Files:**
- Create: `lib/har/types.ts`

- [ ] **Step 1: Write the types** (no test; consumed by later tested modules)

```ts
export type MimeCategory = "document" | "script" | "stylesheet" | "image" | "font" | "xhr" | "media" | "other";

export interface Timings {
  blocked: number; dns: number; connect: number; ssl: number;
  send: number; wait: number; receive: number; total: number;
}
export interface Sizes { headersBytes: number; bodyBytes: number; transferBytes: number; }
export interface HeaderKV { name: string; value: string; }

export interface HarRequest {
  index: number;
  startedMs: number;            // offset from capture start
  url: string; scheme: string; host: string; registrableDomain: string; path: string;
  method: string; status: number; statusText: string; httpVersion: string;
  mimeCategory: MimeCategory; isThirdParty: boolean; fromCache: boolean;
  timings: Timings; sizes: Sizes;
  reqHeaders: HeaderKV[]; resHeaders: HeaderKV[];
  setCookies: string[]; queryParams: string[];
  redirectUrl?: string;
}

export interface ParseResult {
  pageUrl: string; primaryDomain: string; startedMs: number;
  onContentLoadMs?: number; onLoadMs?: number;
  entries: HarRequest[]; total: number; errors: number;
}
```

- [ ] **Step 2: Commit**

```bash
git add lib/har/types.ts
git commit -m "Add Harbor core types"
```

## Task 3: parse helpers (registrableDomain, mimeCategoryOf, normalizeTimings)

**Files:**
- Create: `lib/har/parse.ts` (helpers first), `tests/parse.test.ts`

- [ ] **Step 1: Write failing tests for the helpers**

```ts
import { describe, it, expect } from "vitest";
import { registrableDomain, mimeCategoryOf, normalizeTimings } from "../lib/har/parse";

describe("registrableDomain", () => {
  it("returns eTLD+1, handling multi-part suffixes", () => {
    expect(registrableDomain("www.example.com")).toBe("example.com");
    expect(registrableDomain("a.b.example.co.uk")).toBe("example.co.uk");
    expect(registrableDomain("localhost")).toBe("localhost");
  });
});
describe("mimeCategoryOf", () => {
  it("maps mime types and url hints to categories", () => {
    expect(mimeCategoryOf("text/html", "/")).toBe("document");
    expect(mimeCategoryOf("application/javascript", "/a.js")).toBe("script");
    expect(mimeCategoryOf("text/css", "/a.css")).toBe("stylesheet");
    expect(mimeCategoryOf("image/png", "/a.png")).toBe("image");
    expect(mimeCategoryOf("font/woff2", "/a.woff2")).toBe("font");
    expect(mimeCategoryOf("application/json", "/api")).toBe("xhr");
    expect(mimeCategoryOf("application/octet-stream", "/x")).toBe("other");
  });
});
describe("normalizeTimings", () => {
  it("clamps -1 to 0 and computes total", () => {
    const t = normalizeTimings({ blocked: -1, dns: 5, connect: 10, ssl: -1, send: 1, wait: 100, receive: 4 });
    expect(t.blocked).toBe(0);
    expect(t.total).toBe(120);
  });
});
```

- [ ] **Step 2: Run to verify it fails** — Run: `npm test -- parse` → FAIL (not exported).

- [ ] **Step 3: Implement the helpers in `lib/har/parse.ts`**

```ts
import type { MimeCategory, Timings } from "./types";

const MULTI_SUFFIX = new Set(["co.uk", "com.au", "co.jp", "co.in", "com.br", "co.nz"]);

export function registrableDomain(host: string): string {
  const parts = host.split(".").filter(Boolean);
  if (parts.length <= 2) return host;
  const lastTwo = parts.slice(-2).join(".");
  if (MULTI_SUFFIX.has(lastTwo)) return parts.slice(-3).join(".");
  return lastTwo;
}

export function mimeCategoryOf(mime: string, url: string): MimeCategory {
  const m = (mime || "").toLowerCase();
  if (m.startsWith("text/html") || m.includes("xhtml")) return "document";
  if (m.includes("javascript") || /\.m?js(\?|$)/.test(url)) return "script";
  if (m.includes("css")) return "stylesheet";
  if (m.startsWith("image/")) return "image";
  if (m.startsWith("font/") || /\.(woff2?|ttf|otf|eot)(\?|$)/.test(url)) return "font";
  if (m.startsWith("video/") || m.startsWith("audio/")) return "media";
  if (m.includes("json") || m.includes("xml") || m.includes("x-www-form")) return "xhr";
  return "other";
}

export function normalizeTimings(raw: Partial<Record<keyof Timings, number>> = {}): Timings {
  const c = (n?: number) => (typeof n === "number" && n > 0 ? n : 0);
  const t = { blocked: c(raw.blocked), dns: c(raw.dns), connect: c(raw.connect), ssl: c(raw.ssl), send: c(raw.send), wait: c(raw.wait), receive: c(raw.receive), total: 0 };
  t.total = t.blocked + t.dns + t.connect + t.ssl + t.send + t.wait + t.receive;
  return t;
}
```

- [ ] **Step 4: Run to verify pass** — Run: `npm test -- parse` → PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/har/parse.ts tests/parse.test.ts
git commit -m "Add HAR parse helpers (registrable domain, mime category, timings)"
```

## Task 4: parseHar

**Files:**
- Modify: `lib/har/parse.ts`, `tests/parse.test.ts`

- [ ] **Step 1: Add failing tests for `parseHar`**

```ts
import { parseHar } from "../lib/har/parse";

const HAR = JSON.stringify({
  log: {
    pages: [{ startedDateTime: "2026-02-01T00:00:00.000Z", pageTimings: { onContentLoad: 200, onLoad: 500 } }],
    entries: [
      { startedDateTime: "2026-02-01T00:00:00.000Z", request: { method: "GET", url: "https://example.com/", httpVersion: "h2", headers: [], queryString: [], cookies: [] },
        response: { status: 200, statusText: "OK", httpVersion: "h2", content: { mimeType: "text/html", size: 1000 }, headers: [], cookies: [], _transferSize: 1200 },
        timings: { blocked: 1, dns: 2, connect: 3, ssl: 0, send: 1, wait: 50, receive: 5 }, cache: {} },
      { startedDateTime: "2026-02-01T00:00:00.300Z", request: { method: "GET", url: "https://cdn.other.com/a.js?x=1", httpVersion: "h2", headers: [], queryString: [{ name: "x", value: "1" }], cookies: [] },
        response: { status: 404, statusText: "Not Found", httpVersion: "h2", content: { mimeType: "application/javascript", size: 50 }, headers: [], cookies: [] },
        timings: { blocked: 0, dns: 1, connect: 1, ssl: 0, send: 0, wait: 10, receive: 1 }, cache: {} },
    ],
  },
});

describe("parseHar", () => {
  it("normalizes entries, derives primary domain, third-party flag, errors and page timings", () => {
    const r = parseHar(HAR);
    expect(r.total).toBe(2);
    expect(r.primaryDomain).toBe("example.com");
    expect(r.errors).toBe(1);
    expect(r.onLoadMs).toBe(500);
    expect(r.entries[0].mimeCategory).toBe("document");
    expect(r.entries[0].isThirdParty).toBe(false);
    expect(r.entries[0].startedMs).toBe(0);
    expect(r.entries[1].isThirdParty).toBe(true);
    expect(r.entries[1].startedMs).toBe(300);
    expect(r.entries[1].sizes.transferBytes).toBe(50); // falls back to body size
  });
  it("throws a friendly error on non-HAR JSON", () => {
    expect(() => parseHar('{"foo":1}')).toThrow(/not a valid HAR/i);
    expect(() => parseHar("not json")).toThrow();
  });
});
```

- [ ] **Step 2: Run to verify fail** — Run: `npm test -- parse` → FAIL.

- [ ] **Step 3: Implement `parseHar`** (append to `lib/har/parse.ts`)

```ts
import type { HarRequest, ParseResult, HeaderKV } from "./types";

function toKV(h: { name?: string; value?: string }[] = []): HeaderKV[] {
  return h.filter((x) => x?.name).map((x) => ({ name: String(x.name), value: String(x.value ?? "") }));
}

export function parseHar(text: string): ParseResult {
  let root: any;
  try { root = JSON.parse(text); } catch { throw new Error("File is not valid JSON."); }
  const log = root?.log;
  if (!log || !Array.isArray(log.entries)) throw new Error("File is not a valid HAR (missing log.entries).");
  const rawEntries: any[] = log.entries;
  const startTimes = rawEntries.map((e) => Date.parse(e.startedDateTime)).filter((n) => !Number.isNaN(n));
  const startedMs = startTimes.length ? Math.min(...startTimes) : 0;

  const hostCount = new Map<string, number>();
  const entries: HarRequest[] = rawEntries.map((e, index) => {
    const url: string = e.request?.url ?? "";
    let u: URL; try { u = new URL(url); } catch { u = new URL("https://invalid.local/"); }
    hostCount.set(u.hostname, (hostCount.get(u.hostname) ?? 0) + 1);
    const status = Number(e.response?.status ?? 0);
    const bodyBytes = Math.max(0, Number(e.response?.content?.size ?? 0));
    const transfer = Number(e.response?._transferSize);
    const headersBytes = Math.max(0, Number(e.response?.headersSize ?? 0));
    return {
      index,
      startedMs: (Date.parse(e.startedDateTime) || startedMs) - startedMs,
      url, scheme: u.protocol.replace(":", ""), host: u.hostname, registrableDomain: registrableDomain(u.hostname), path: u.pathname,
      method: e.request?.method ?? "GET", status, statusText: e.response?.statusText ?? "", httpVersion: e.response?.httpVersion ?? "",
      mimeCategory: mimeCategoryOf(e.response?.content?.mimeType ?? "", url),
      isThirdParty: false, // set below once primary domain known
      fromCache: Boolean(e.cache && (e.cache.afterRequest || e.cache.beforeRequest)) || transfer === 0,
      timings: normalizeTimings(e.timings ?? {}),
      sizes: { headersBytes, bodyBytes, transferBytes: Number.isFinite(transfer) && transfer >= 0 ? transfer : bodyBytes },
      reqHeaders: toKV(e.request?.headers), resHeaders: toKV(e.response?.headers),
      setCookies: toKV(e.response?.headers).filter((h) => h.name.toLowerCase() === "set-cookie").map((h) => h.value),
      queryParams: (e.request?.queryString ?? []).map((q: any) => String(q.name)),
      redirectUrl: e.response?.redirectURL || undefined,
    };
  });

  const pageHost = rawEntries[0] ? new URL(entries[0].url).hostname : "";
  let primaryHost = pageHost;
  if (!primaryHost) { let max = -1; for (const [h, n] of hostCount) if (n > max) { max = n; primaryHost = h; } }
  const primaryDomain = registrableDomain(primaryHost);
  for (const r of entries) r.isThirdParty = r.registrableDomain !== primaryDomain;

  const page = log.pages?.[0];
  return {
    pageUrl: entries[0]?.url ?? "", primaryDomain, startedMs,
    onContentLoadMs: page?.pageTimings?.onContentLoad > 0 ? page.pageTimings.onContentLoad : undefined,
    onLoadMs: page?.pageTimings?.onLoad > 0 ? page.pageTimings.onLoad : undefined,
    entries, total: entries.length,
    errors: entries.filter((r) => r.status === 0 || r.status >= 400).length,
  };
}
```

- [ ] **Step 4: Run to verify pass** — Run: `npm test -- parse` → PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/har/parse.ts tests/parse.test.ts
git commit -m "Add parseHar: normalize HAR into the Harbor request model"
```

## Task 5: waterfall

**Files:**
- Create: `lib/har/waterfall.ts`, `tests/waterfall.test.ts`

- [ ] **Step 1: Failing test**

```ts
import { describe, it, expect } from "vitest";
import { buildWaterfall } from "../lib/har/waterfall";
import { parseHar } from "../lib/har/parse";
import { SAMPLE_HAR } from "../lib/har/sample"; // created in Task 12; until then inline a small HAR string

// Use a minimal inline HAR for this test to avoid ordering dependency on Task 12.
const HAR = JSON.stringify({ log: { entries: [
  { startedDateTime: "2026-02-01T00:00:00.000Z", request: { method:"GET", url:"https://example.com/", headers:[], queryString:[], cookies:[] }, response: { status:200, content:{mimeType:"text/html",size:1000}, headers:[], cookies:[] }, timings:{blocked:0,dns:0,connect:0,ssl:0,send:0,wait:100,receive:10}, cache:{} },
  { startedDateTime: "2026-02-01T00:00:00.050Z", request: { method:"GET", url:"https://example.com/a.css", headers:[], queryString:[], cookies:[] }, response: { status:200, content:{mimeType:"text/css",size:500}, headers:[], cookies:[] }, timings:{blocked:0,dns:0,connect:0,ssl:0,send:0,wait:300,receive:10}, cache:{} },
] } });

describe("buildWaterfall", () => {
  it("computes rows with offsets, slowest ordering and page load time", () => {
    const w = buildWaterfall(parseHar(HAR));
    expect(w.rows).toHaveLength(2);
    expect(w.rows[1].offsetMs).toBe(50);
    expect(w.slowest[0].timings.total).toBe(310); // the css is slowest
    expect(w.pageLoadMs).toBeGreaterThanOrEqual(360); // 50 + 310
  });
});
```

- [ ] **Step 2: Run to verify fail** — Run: `npm test -- waterfall` → FAIL.

- [ ] **Step 3: Implement `lib/har/waterfall.ts`**

```ts
import type { HarRequest, ParseResult } from "./types";

export interface WaterfallRow { req: HarRequest; offsetMs: number; endMs: number; }
export interface Waterfall { rows: WaterfallRow[]; pageLoadMs: number; slowest: HarRequest[]; renderBlocking: HarRequest[]; criticalPathMs: number; }

export function buildWaterfall(p: ParseResult): Waterfall {
  const rows: WaterfallRow[] = p.entries.map((req) => ({ req, offsetMs: req.startedMs, endMs: req.startedMs + req.timings.total }));
  const pageLoadMs = p.onLoadMs ?? (rows.length ? Math.max(...rows.map((r) => r.endMs)) : 0);
  const slowest = [...p.entries].sort((a, b) => b.timings.total - a.timings.total).slice(0, 10);
  // Heuristic: render-blocking = stylesheets / head scripts that finish before the document response completes.
  const doc = p.entries.find((r) => r.mimeCategory === "document");
  const docEnd = doc ? doc.startedMs + doc.timings.total : 0;
  const renderBlocking = p.entries.filter((r) => (r.mimeCategory === "stylesheet" || r.mimeCategory === "script") && r.startedMs + r.timings.total <= docEnd + 1);
  const criticalPathMs = rows.filter((r) => !r.req.isThirdParty).reduce((m, r) => Math.max(m, r.endMs), 0);
  return { rows, pageLoadMs, slowest, renderBlocking, criticalPathMs };
}
```

- [ ] **Step 4: Run to verify pass** — Run: `npm test -- waterfall` → PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/har/waterfall.ts tests/waterfall.test.ts
git commit -m "Add waterfall geometry: offsets, slowest, render-blocking heuristic"
```

## Task 6: weight

**Files:**
- Create: `lib/har/weight.ts`, `tests/weight.test.ts`

- [ ] **Step 1: Failing test**

```ts
import { describe, it, expect } from "vitest";
import { analyzeWeight } from "../lib/har/weight";
import { parseHar } from "../lib/har/parse";

const HAR = JSON.stringify({ log: { entries: [
  { startedDateTime:"2026-02-01T00:00:00.000Z", request:{method:"GET",url:"https://example.com/",headers:[],queryString:[],cookies:[]}, response:{status:200,content:{mimeType:"text/html",size:1000},headers:[],cookies:[],_transferSize:1000}, timings:{wait:10,receive:1}, cache:{} },
  { startedDateTime:"2026-02-01T00:00:00.010Z", request:{method:"GET",url:"https://cdn.other.com/a.js",headers:[],queryString:[],cookies:[]}, response:{status:200,content:{mimeType:"application/javascript",size:2000},headers:[],cookies:[],_transferSize:2000}, timings:{wait:10,receive:1}, cache:{} },
] } });

describe("analyzeWeight", () => {
  it("aggregates bytes by category and domain with first/third split", () => {
    const w = analyzeWeight(parseHar(HAR));
    expect(w.totalBytes).toBe(3000);
    expect(w.firstPartyBytes).toBe(1000);
    expect(w.thirdPartyBytes).toBe(2000);
    expect(w.byCategory.find((c) => c.category === "script")!.bytes).toBe(2000);
    expect(w.byDomain[0].bytes).toBe(2000); // largest first
    expect(w.biggestOffenders[0].sizes.transferBytes).toBe(2000);
  });
});
```

- [ ] **Step 2: Run to verify fail.**

- [ ] **Step 3: Implement `lib/har/weight.ts`**

```ts
import type { HarRequest, MimeCategory, ParseResult } from "./types";

export interface WeightBreakdown {
  totalBytes: number; firstPartyBytes: number; thirdPartyBytes: number;
  byCategory: { category: MimeCategory; bytes: number; count: number }[];
  byDomain: { domain: string; bytes: number; count: number }[];
  biggestOffenders: HarRequest[];
  estimatedCompressionSavings: number;
}

const TEXT: MimeCategory[] = ["document", "script", "stylesheet", "xhr"];

export function analyzeWeight(p: ParseResult): WeightBreakdown {
  const cat = new Map<MimeCategory, { bytes: number; count: number }>();
  const dom = new Map<string, { bytes: number; count: number }>();
  let total = 0, first = 0, third = 0, savings = 0;
  for (const r of p.entries) {
    const b = r.sizes.transferBytes; total += b;
    if (r.isThirdParty) third += b; else first += b;
    const c = cat.get(r.mimeCategory) ?? { bytes: 0, count: 0 }; c.bytes += b; c.count++; cat.set(r.mimeCategory, c);
    const d = dom.get(r.registrableDomain) ?? { bytes: 0, count: 0 }; d.bytes += b; d.count++; dom.set(r.registrableDomain, d);
    const enc = r.resHeaders.find((h) => h.name.toLowerCase() === "content-encoding")?.value ?? "";
    if (TEXT.includes(r.mimeCategory) && !/gzip|br|deflate/i.test(enc) && r.sizes.bodyBytes > 1000) savings += Math.round(r.sizes.bodyBytes * 0.7);
  }
  return {
    totalBytes: total, firstPartyBytes: first, thirdPartyBytes: third,
    byCategory: [...cat.entries()].map(([category, v]) => ({ category, ...v })).sort((a, b) => b.bytes - a.bytes),
    byDomain: [...dom.entries()].map(([domain, v]) => ({ domain, ...v })).sort((a, b) => b.bytes - a.bytes),
    biggestOffenders: [...p.entries].sort((a, b) => b.sizes.transferBytes - a.sizes.transferBytes).slice(0, 10),
    estimatedCompressionSavings: savings,
  };
}
```

- [ ] **Step 4: Run to verify pass.**

- [ ] **Step 5: Commit** — `git commit -m "Add weight analysis: bytes by type/domain, first/third split, compression estimate"`

## Task 7: thirdparty

**Files:** Create `lib/har/thirdparty.ts`, `tests/thirdparty.test.ts`

- [ ] **Step 1: Failing test**

```ts
import { describe, it, expect } from "vitest";
import { analyzeThirdParties, classifyDomain } from "../lib/har/thirdparty";
import { parseHar } from "../lib/har/parse";

describe("classifyDomain", () => {
  it("classifies known third parties", () => {
    expect(classifyDomain("google-analytics.com")).toBe("analytics");
    expect(classifyDomain("doubleclick.net")).toBe("ads");
    expect(classifyDomain("fonts.gstatic.com")).toBe("fonts");
    expect(classifyDomain("random.example")).toBe("unknown");
  });
});

const HAR = JSON.stringify({ log: { entries: [
  { startedDateTime:"2026-02-01T00:00:00.000Z", request:{method:"GET",url:"https://example.com/",headers:[],queryString:[],cookies:[]}, response:{status:200,content:{mimeType:"text/html",size:100},headers:[],cookies:[]}, timings:{wait:10,receive:1}, cache:{} },
  { startedDateTime:"2026-02-01T00:00:00.010Z", request:{method:"GET",url:"https://google-analytics.com/collect?v=1",headers:[],queryString:[{name:"v",value:"1"}],cookies:[]}, response:{status:200,content:{mimeType:"image/gif",size:50},headers:[],cookies:[]}, timings:{wait:5,receive:1}, cache:{} },
] } });

describe("analyzeThirdParties", () => {
  it("groups third parties with bytes, time and category", () => {
    const tp = analyzeThirdParties(parseHar(HAR));
    expect(tp).toHaveLength(1);
    expect(tp[0].domain).toBe("google-analytics.com");
    expect(tp[0].category).toBe("analytics");
    expect(tp[0].requests).toBe(1);
    expect(tp[0].sentQueryParams).toBe(true);
  });
});
```

- [ ] **Step 2: Run to verify fail.**

- [ ] **Step 3: Implement `lib/har/thirdparty.ts`**

```ts
import type { ParseResult } from "./types";

export type TPCategory = "analytics" | "ads" | "tag-manager" | "cdn" | "social" | "fonts" | "unknown";
export interface ThirdParty { domain: string; category: TPCategory; requests: number; bytes: number; timeMs: number; sentCookies: boolean; sentQueryParams: boolean; }

const RULES: [RegExp, TPCategory][] = [
  [/google-analytics|analytics|segment|mixpanel|amplitude|hotjar|plausible/, "analytics"],
  [/doubleclick|adservice|adsystem|adnxs|criteo|taboola|outbrain/, "ads"],
  [/googletagmanager|tagmanager/, "tag-manager"],
  [/cloudflare|akamai|fastly|cloudfront|jsdelivr|unpkg|cdn\./, "cdn"],
  [/facebook|twitter|linkedin|tiktok|instagram|connect\.facebook/, "social"],
  [/fonts\.(googleapis|gstatic)|typekit|fontawesome/, "fonts"],
];
export function classifyDomain(domain: string): TPCategory {
  for (const [re, cat] of RULES) if (re.test(domain)) return cat;
  return "unknown";
}

export function analyzeThirdParties(p: ParseResult): ThirdParty[] {
  const m = new Map<string, ThirdParty>();
  for (const r of p.entries) {
    if (!r.isThirdParty) continue;
    const tp = m.get(r.registrableDomain) ?? { domain: r.registrableDomain, category: classifyDomain(r.registrableDomain), requests: 0, bytes: 0, timeMs: 0, sentCookies: false, sentQueryParams: false };
    tp.requests++; tp.bytes += r.sizes.transferBytes; tp.timeMs += r.timings.total;
    if (r.reqHeaders.some((h) => h.name.toLowerCase() === "cookie")) tp.sentCookies = true;
    if (r.queryParams.length) tp.sentQueryParams = true;
    m.set(r.registrableDomain, tp);
  }
  return [...m.values()].sort((a, b) => b.bytes - a.bytes);
}
```

- [ ] **Step 4: Run to verify pass.**

- [ ] **Step 5: Commit** — `git commit -m "Add third-party grouping and classification"`

## Task 8: privacy (secrets / PII / trackers)

**Files:** Create `lib/har/privacy.ts`, `tests/privacy.test.ts`

- [ ] **Step 1: Failing test**

```ts
import { describe, it, expect } from "vitest";
import { scanPrivacy, maskSecret } from "../lib/har/privacy";
import { parseHar } from "../lib/har/parse";

describe("maskSecret", () => {
  it("masks all but the last 4 chars", () => {
    expect(maskSecret("abcdefgh.ijkl")).toBe("…ijkl");
  });
});

const HAR = JSON.stringify({ log: { entries: [
  { startedDateTime:"2026-02-01T00:00:00.000Z", request:{method:"GET",url:"https://example.com/?email=jane@acme.com",headers:[{name:"Authorization",value:"Bearer eyJhbGciOiJ.payload.sig"}],queryString:[{name:"email",value:"jane@acme.com"}],cookies:[]}, response:{status:200,content:{mimeType:"text/html",size:10},headers:[],cookies:[]}, timings:{wait:1,receive:1}, cache:{} },
  { startedDateTime:"2026-02-01T00:00:00.010Z", request:{method:"GET",url:"https://doubleclick.net/p?id=1",headers:[],queryString:[],cookies:[]}, response:{status:200,content:{mimeType:"image/gif",size:1},headers:[],cookies:[]}, timings:{wait:1,receive:1}, cache:{} },
] } });

describe("scanPrivacy", () => {
  it("flags bearer tokens, emails (PII) and tracker requests, with masked evidence", () => {
    const f = scanPrivacy(parseHar(HAR));
    const kinds = f.map((x) => x.kind);
    expect(kinds).toContain("secret");
    expect(kinds).toContain("pii");
    expect(kinds).toContain("tracker");
    const secret = f.find((x) => x.kind === "secret")!;
    expect(secret.evidence).not.toContain("eyJhbGciOiJ"); // masked
    expect(secret.severity).toBe("high");
  });
});
```

- [ ] **Step 2: Run to verify fail.**

- [ ] **Step 3: Implement `lib/har/privacy.ts`**

```ts
import type { ParseResult } from "./types";
import { classifyDomain } from "./thirdparty";

export type Severity = "high" | "medium" | "low";
export interface PrivacyFinding { severity: Severity; kind: "secret" | "pii" | "tracker"; title: string; where: string; evidence: string; }

export function maskSecret(s: string): string { return "…" + s.slice(-4); }

const JWT = /eyJ[A-Za-z0-9_-]{5,}\.[A-Za-z0-9_-]{5,}\.[A-Za-z0-9_-]{2,}/;
const BEARER = /bearer\s+[A-Za-z0-9._-]{10,}/i;
const APIKEY = /(api[_-]?key|secret|token)["'=:\s]+[A-Za-z0-9._-]{12,}/i;
const EMAIL = /[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}/;

export function scanPrivacy(p: ParseResult): PrivacyFinding[] {
  const out: PrivacyFinding[] = [];
  for (const r of p.entries) {
    const hay = [r.url, ...r.reqHeaders.map((h) => `${h.name}: ${h.value}`)];
    for (const s of hay) {
      const m = s.match(JWT) || s.match(BEARER) || s.match(APIKEY);
      if (m) { out.push({ severity: "high", kind: "secret", title: "Token or secret in request", where: r.url.slice(0, 80), evidence: maskSecret(m[0]) }); break; }
    }
    const em = (r.url + " " + r.queryParams.join(" ")).match(EMAIL);
    if (em) out.push({ severity: "medium", kind: "pii", title: "Email address exposed", where: r.url.slice(0, 80), evidence: maskSecret(em[0]) });
    if (r.isThirdParty && (classifyDomain(r.registrableDomain) === "ads" || classifyDomain(r.registrableDomain) === "analytics")) {
      out.push({ severity: "low", kind: "tracker", title: `Tracker request to ${r.registrableDomain}`, where: r.url.slice(0, 80), evidence: r.registrableDomain });
    }
  }
  const rank = { high: 0, medium: 1, low: 2 };
  return out.sort((a, b) => rank[a.severity] - rank[b.severity]);
}
```

- [ ] **Step 4: Run to verify pass.**

- [ ] **Step 5: Commit** — `git commit -m "Add privacy scan: secrets, PII and trackers with masked evidence"`

## Task 9: analyze orchestrator

**Files:** Create `lib/har/analyze.ts`, `tests/analyze.test.ts`

- [ ] **Step 1: Failing test**

```ts
import { describe, it, expect } from "vitest";
import { analyze } from "../lib/har/analyze";
import { parseHar } from "../lib/har/parse";

const HAR = JSON.stringify({ log: { entries: [
  { startedDateTime:"2026-02-01T00:00:00.000Z", request:{method:"GET",url:"https://example.com/",headers:[],queryString:[],cookies:[]}, response:{status:200,content:{mimeType:"text/html",size:1000},headers:[],cookies:[],_transferSize:1000}, timings:{wait:50,receive:5}, cache:{} },
  { startedDateTime:"2026-02-01T00:00:00.060Z", request:{method:"GET",url:"https://cdn.other.com/a.js",headers:[],queryString:[],cookies:[]}, response:{status:500,content:{mimeType:"application/javascript",size:2000},headers:[],cookies:[],_transferSize:2000}, timings:{wait:20,receive:2}, cache:{} },
] } });

describe("analyze", () => {
  it("produces a summary plus every section", () => {
    const a = analyze(parseHar(HAR));
    expect(a.summary.totalRequests).toBe(2);
    expect(a.summary.totalTransferBytes).toBe(3000);
    expect(a.summary.errors).toBe(1);
    expect(a.summary.firstPartyBytes).toBe(1000);
    expect(a.waterfall.rows).toHaveLength(2);
    expect(a.weight.totalBytes).toBe(3000);
    expect(Array.isArray(a.thirdParties)).toBe(true);
    expect(Array.isArray(a.privacy)).toBe(true);
  });
});
```

- [ ] **Step 2: Run to verify fail.**

- [ ] **Step 3: Implement `lib/har/analyze.ts`**

```ts
import type { ParseResult } from "./types";
import { buildWaterfall, type Waterfall } from "./waterfall";
import { analyzeWeight, type WeightBreakdown } from "./weight";
import { analyzeThirdParties, type ThirdParty } from "./thirdparty";
import { scanPrivacy, type PrivacyFinding } from "./privacy";

export interface Summary {
  totalRequests: number; totalTransferBytes: number; pageLoadMs: number; ttfbMs: number;
  errors: number; cacheHitRatio: number; firstPartyBytes: number; thirdPartyBytes: number;
}
export interface Analysis {
  summary: Summary; waterfall: Waterfall; weight: WeightBreakdown;
  thirdParties: ThirdParty[]; privacy: PrivacyFinding[];
}

export function analyze(p: ParseResult): Analysis {
  const waterfall = buildWaterfall(p);
  const weight = analyzeWeight(p);
  const doc = p.entries.find((r) => r.mimeCategory === "document");
  const cached = p.entries.filter((r) => r.fromCache).length;
  return {
    summary: {
      totalRequests: p.total, totalTransferBytes: weight.totalBytes, pageLoadMs: waterfall.pageLoadMs,
      ttfbMs: doc ? doc.timings.blocked + doc.timings.dns + doc.timings.connect + doc.timings.ssl + doc.timings.send + doc.timings.wait : 0,
      errors: p.errors, cacheHitRatio: p.total ? cached / p.total : 0,
      firstPartyBytes: weight.firstPartyBytes, thirdPartyBytes: weight.thirdPartyBytes,
    },
    waterfall, weight, thirdParties: analyzeThirdParties(p), privacy: scanPrivacy(p),
  };
}
```

- [ ] **Step 4: Run to verify pass.**

- [ ] **Step 5: Commit** — `git commit -m "Add analyze orchestrator producing the unified Analysis"`

## Task 10: summary (Markdown export)

**Files:** Create `lib/har/summary.ts`, `tests/summary.test.ts`

- [ ] **Step 1: Failing test** — assert `toMarkdown(name, parse, analysis)` returns a string containing `# Harbor report — <name>`, a summary table, a "## Slowest requests" section, "## Weight" and "## Third parties", and is deterministic (same input → identical output). Omits empty sections (e.g. no "## Privacy findings" when `privacy` is empty).

- [ ] **Step 2: Run to verify fail.**

- [ ] **Step 3: Implement `toMarkdown`** mirroring Logloom's `lib/log/summary.ts` structure: a title, a key/value summary list (requests, transferred KB, load time, errors, cache ratio, first/third split), then sections built only when their arrays are non-empty, ending with the "Generated by Harbor. Parsed entirely in the browser; nothing was uploaded." footer.

- [ ] **Step 4: Run to verify pass.**

- [ ] **Step 5: Commit** — `git commit -m "Add shareable Markdown report (toMarkdown)"`

## Task 11: copilot (grounded BYO-key AI)

**Files:** Create `lib/ai/copilot.ts`, `tests/copilot.test.ts`

- [ ] **Step 1: Failing test** — port Logloom's `tests/copilot.test.ts`: assert `analysisContext(summaryFacts, analysis)` contains the score-free facts (requests, total KB, load time, top third parties, top privacy findings, slowest requests); `groundedSystem` contains "Answer ONLY from the FACTS" and "FACTS:"; `pickProvider("sk-ant-...")==="anthropic"`, else `"gemini"`; `extractTextDeltas` pulls `content_block_delta` text and tolerates partial JSON; `extractGeminiTextDeltas` pulls candidate part text.

- [ ] **Step 2: Run to verify fail.**

- [ ] **Step 3: Implement `lib/ai/copilot.ts`** by porting Logloom's copilot verbatim in structure (`ChatMessage`, `AskOptions`, `analysisContext`, `groundedSystem`, `pickProvider`, `extractTextDeltas`, `extractGeminiTextDeltas`, `pump`, `streamAnthropic`, `streamGemini`, `streamCopilot`; defaults `claude-haiku-4-5-20251001` / `gemini-2.0-flash`). Replace the log-specific `analysisContext` body with Harbor facts: format the `Summary`, top `weight.byCategory`, top `thirdParties`, `privacy` count by severity, and `waterfall.slowest` into the FACTS block. The system prompt: "You are Harbor, an expert web-performance and privacy engineer analyzing a network capture. Answer ONLY from the FACTS. Cite specific requests, domains, and millisecond/byte figures. Be concise and concrete."

- [ ] **Step 4: Run to verify pass.**

- [ ] **Step 5: Commit** — `git commit -m "Add grounded BYO-key AI copilot (Anthropic + Gemini, streaming)"`

## Task 12: sample + worker + runParse

**Files:** Create `lib/har/sample.ts`, `lib/har/worker.ts`, `lib/har/runParse.ts`

- [ ] **Step 1 (sample):** Create `lib/har/sample.ts` exporting `SAMPLES: {id,name,label,description,make:()=>string}[]` with two deterministically generated HAR strings (a slow, third-party-heavy page and a lean optimized page). Generate timestamps from a fixed base (no `Date.now`). Also export `SAMPLE_HAR = SAMPLES[0].make()` for tests.

- [ ] **Step 2 (worker):** Create `lib/har/worker.ts` mirroring Logloom's: `self.onmessage` runs `parseHar` + `analyze`, posts `{ ok:true, parse, analysis }` or `{ ok:false, error }`.

- [ ] **Step 3 (runParse):** Create `lib/har/runParse.ts` mirroring Logloom's `runParse`: prefer a module Worker (`new Worker(new URL("./worker.ts", import.meta.url), {type:"module"})`), fall back to synchronous `parseHar`+`analyze`, return `{ parse, analysis }`. Add a `tests/runParse.test.ts` that calls it in the node env (Worker undefined → sync path) and asserts it resolves with a valid `analysis`.

- [ ] **Step 4: Run** `npm test` → all green.

- [ ] **Step 5: Commit** — `git commit -m "Add sample HAR library, parse worker and runParse fallback"`

## Task 13: dashboard UI

**Files:** Create `app/page.tsx`, `components/Waterfall.tsx`, `components/WeightBreakdown.tsx`, `components/ThirdParties.tsx`, `components/Findings.tsx`, `components/Copilot.tsx`. Modify `app/globals.css` if needed.

- [ ] **Step 1:** Build `components/Copilot.tsx` by porting Logloom's component (BYO-key input, suggestion chips tailored to Harbor: "What's the biggest performance problem?", "Which third parties should I drop?", "Any secrets leaking?", "What should I fix first?"; streaming multi-turn; stop button; grounded via `groundedSystem(summary, analysis)`).

- [ ] **Step 2:** Build `components/Waterfall.tsx`: given `Waterfall`, render each row as a horizontal bar positioned by `offsetMs` over `pageLoadMs`, segmented by timing phase color, label = path, hover title = per-phase ms. Highlight slowest rows. (Static in Phase 1; interactivity added in Phase 3.)

- [ ] **Step 3:** Build `WeightBreakdown.tsx` (stacked bar by category + per-domain list + biggest offenders), `ThirdParties.tsx` (ranked list), `Findings.tsx` (severity-ranked privacy findings with masked evidence).

- [ ] **Step 4:** Build `app/page.tsx` (port Logloom's page shell): empty state with dropzone + paste + sample buttons from `SAMPLES`; on load, `runParse` with a "Parsing…" busy state storing `{name, parse, analysis}`; dashboard renders summary header + `Waterfall` + `WeightBreakdown` + `ThirdParties` + `Findings` + `Copilot` + a "Download summary" button calling `toMarkdown` and saving a blob; "new file" reset.

- [ ] **Step 5:** Run `npm run typecheck && npm test && npm run build` → all green. Manually verify a sample loads and the waterfall renders.

- [ ] **Step 6: Commit** — `git commit -m "Add Harbor dashboard: waterfall, weight, third parties, findings, copilot, export"`

## Task 14: README + verify Phase 1

- [ ] **Step 1:** Write `README.md` (what it is, privacy stance, how to use, stack, test count, "Phase 1 of N" status).
- [ ] **Step 2:** Run full verification: `npm run typecheck && npm test && npm run build`.
- [ ] **Step 3: Commit** — `git commit -m "Add README; Phase 1 (MVP core) complete"`

**Phase 1 done.** Optional: create the public repo + Vercel deploy here (only on the user's explicit go), per portfolio conventions.

---

# Phase 2: Audit & score (task outline)

Expand each into full TDD steps at phase start.

- **Task 2.1 `lib/har/audit.ts` — recommendations.** `audit(analysis): AuditResult`. TDD each rule with a crafted `Analysis`: uncompressed-text (uses `weight.estimatedCompressionSavings`), render-blocking resources (`waterfall.renderBlocking`), too many requests, heavy third parties, large images, missing-cache (Phase 4 feeds this; until then skip), errors present. Each recommendation `{ id, title, detail, impact, severity, count }`, sorted by severity then impact. Assert specific recommendation ids fire for specific inputs.
- **Task 2.2 score + budgets.** `score(analysis): {value:0..100, grade}` weighted blend (bytes 30%, requests 20%, ttfb 20%, load 30% against reference curves); `budgets(analysis, cfg?)` returns pass/fail per metric with defaults (e.g. ≤1.6MB, ≤50 requests, ≤500KB third-party, ≤3s load). TDD: a lean capture scores high/passes; a bloated one scores low/fails.
- **Task 2.3 wire into analyze + copilot.** Add `audit` to `Analysis`; include top recommendations + score in `analysisContext`. Update analyze test.
- **Task 2.4 UI.** `components/ScoreHeader.tsx` (gauge + grade + budget chips), `components/Recommendations.tsx` (ranked list with savings). Insert at top of dashboard.
- **Task 2.5** verify + commit per task; phase commit "Phase 2: deterministic audit, score and budgets".

# Phase 3: Interactive waterfall (task outline)

- **Task 3.1 filter/sort state.** Pure `lib/har/filter.ts`: `applyFilters(rows, {type?,domain?,status?,query?,sort?})`. TDD filtering/sorting/search.
- **Task 3.2 URL-hash state.** `lib/har/viewState.ts`: `encode/decode` filters + selected index to/from a hash string. TDD round-trip.
- **Task 3.3 Waterfall controls UI.** filter dropdowns, search box, sortable; reads/writes hash.
- **Task 3.4 `components/RequestPanel.tsx`.** drill-down for a selected request: redacted headers (mask `authorization`/`cookie`/`set-cookie` via `maskSecret`), timing phase breakdown, cookies, sizes. Selecting a row opens it; index in hash.
- **Task 3.5** verify + commit; phase commit "Phase 3: interactive waterfall with drill-down and linkable state".

# Phase 4: Deep security/privacy + caching + protocol (task outline)

- **Task 4.1 `lib/har/security.ts`.** `auditSecurity(parse)`: missing HSTS/CSP/X-Content-Type-Options/X-Frame-Options on the document; mixed content (http subresource on https doc); insecure cookies (Set-Cookie lacking Secure/HttpOnly/SameSite). TDD each with crafted headers.
- **Task 4.2 `lib/har/caching.ts`.** `analyzeCaching(parse)`: cache-hit ratio, cacheable-without-headers list, repeat-view bytes/requests saved (cacheable resources counted as cached on revisit). TDD the repeat-view math.
- **Task 4.3 `lib/har/protocol.ts`.** `analyzeProtocol(parse)`: http version counts, redirect chains (follow 3xx by `redirectUrl`/Location), per-host connection reuse. TDD redirect-chain detection.
- **Task 4.4 extend `privacy.ts`.** cookie-sync (same id value sent to ≥2 third parties), fingerprinting-script heuristic (known fp domains/libs). TDD.
- **Task 4.5 wire into analyze + audit + copilot;** add missing-cache + insecure-cookie + h1-only recommendations to `audit.ts`.
- **Task 4.6 UI:** merge into `Findings.tsx` (security+privacy) and add a "Caching & protocol" section.
- **Task 4.7** verify + commit; phase commit "Phase 4: security, caching and protocol analysis".

# Phase 5: Compare mode (task outline)

- **Task 5.1 `lib/har/diff.ts`.** `diff(a, b)`: requests added/removed (by method+path), metric deltas (bytes, requests, load, score), faster/slower lists, budget/score movement. TDD with two analyses.
- **Task 5.2 `app/compare/page.tsx`.** two dropzones → two `runParse` → render `diff` (delta cards, score movement, added/removed/faster/slower tables). Link from main page.
- **Task 5.3** verify + commit; phase commit "Phase 5: before/after compare mode".

# Phase 6: Polish & ship (task outline)

- **Task 6.1** JSON export ("Export data" → `Analysis` as a downloaded `.json`) and PNG export (render the waterfall to a canvas, download). 
- **Task 6.2** a11y + responsive pass: labeled controls, focus-visible states, keyboard nav for the waterfall list and request panel, mobile layout.
- **Task 6.3** expand the sample library; finalize README with live URL + a demo GIF TODO.
- **Task 6.4** full verify (`typecheck`/`test`/`build`); create public repo `KP-MobileTechie/harbor` + deploy to Vercel (only on explicit user go), per conventions. Phase commit "Phase 6: polish, exports and ship".

---

## Self-review notes

- **Spec coverage:** every spec module maps to a task — parse/types (T2-4), waterfall (T5), weight (T6), thirdparty (T7), privacy (T8, extended P4), security/caching/protocol (P4), audit/score/budgets (P2), diff (P5), analyze (T9), summary (T10), copilot (T11), worker/runParse/sample (T12), UI sections + export (T13, P2-P6), compare (P5), exports/a11y/deploy (P6).
- **Type consistency:** `Analysis` is introduced in T9 with `{summary, waterfall, weight, thirdParties, privacy}` and extended additively in P2 (`audit`) and P4 (`security, caching, protocol`); `analysisContext`/copilot consume `Summary` + `Analysis`; `maskSecret` defined in T8 is reused by the RequestPanel redaction in P3.4 and is exported from `privacy.ts`.
- **Heuristics** (critical path, render-blocking, compression estimate, repeat-view) are computed deterministically and labeled as estimates in UI copy and the spec.
```
